# Derived from the .jsonl archive by `capture_ingest.py replay-export`, and
# regenerated by `make corpus`. The JSONL is the source of truth and IS tracked:
# a capture cannot be re-taken, so losing one loses it permanently.
*.replay
